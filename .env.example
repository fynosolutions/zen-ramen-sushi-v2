# Public build-time flag. Keep false on review deployments.
# Set true only when the approved site is serving its final canonical domain.
NEXT_PUBLIC_SITE_INDEXABLE=false

# Google Analytics 4 衡量ID(G-XXXXXXXXXX)。留空=完全不加载分析脚本。
# 本地预览(localhost/127.0.0.1)即使配了也不上报,避免把自己的访问算进去。
NEXT_PUBLIC_GA4_ID=
